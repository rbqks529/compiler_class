// Test 10: 복합 프로그램 - 피보나치
// 목적: 함수, 재귀, 조건문 통합 테스트

fun fib(n: Int): Int {
    if (n <= 1) {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

for (i in 0..10) {
    println(fib(i));
}
